spring:
  datasource:
    url: jdbc:mysql://localhost:3306/video_player_db
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
  data:
    elasticsearch:
      cluster-nodes: localhost:9200
    mongodb:
      uri: mongodb://localhost:27017/video_player_db
  redis:
    host: localhost
    port: 6379
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

minio:
  endpoint: http://localhost:9000
  accessKey: minioadmin
  secretKey: minioadmin
  bucket: video-bucket

mybatis-plus:
  mapper-locations: classpath*:/mapper/**Mapper.xml

